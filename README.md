# test-legacy-mainframe-app

Placeholder tree name used by the public resume site for folder links (`../test-legacy-mainframe-app/`). Replace or symlink this directory to match how you want local “Open folder” navigation to behave.
